# Smart Farm Advisory - Python Dependencies
# Install with: python -m pip install -r requirements.txt

# Core dependencies
groq==1.1.2
requests==2.33.1
keyboard==0.13.5
nltk==3.9.4
python-dotenv==1.0.1

# Flask server (for Vercel API)
flask==3.1.3
flask-cors==6.0.2

# Audio (CLI mode only - not needed for Vercel)
# pyaudio==0.2.14

# Optional: For better audio playback
# pygame==2.5.2
